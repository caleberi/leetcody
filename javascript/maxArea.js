var maxArea = function(heights=[]) {
    if (!heights.length)
        return 0;
    let maxArea = 0;
    let currentHeightIdx = 0;
    while (currentHeightIdx<=heights.length){
        currentHeightIdx++;
    }

    return maxArea;
};